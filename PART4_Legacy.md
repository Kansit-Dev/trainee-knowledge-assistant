# Part 4: Legacy System Thinking

---

## คำถามที่ 1: "น้องจะเริ่มจากตรงไหน"

### 3 วันแรก — Discovery ก่อน แก้ทีหลัง

ก่อนอื่นเลย ผมจะบอกพีตรงๆ ว่า: ผมไม่เคยเจอ legacy PHP บน Windows Server มาก่อน
แต่นั่นไม่ได้แปลว่าผมทำอะไรไม่ได้ แค่แปลว่าผมต้องระวังมากกว่าคนที่เคยเจอ
เพราะสิ่งที่กลัวที่สุดคือ "แก้สิ่งที่ไม่เข้าใจแล้วทำระบบพัง" ในขณะที่ยังมี user ใช้อยู่

---

### วันที่ 1 — ฟังและทำแผนที่ก่อน ไม่แตะโค้ดเลย

**สิ่งแรกที่จะทำ:** ขอนั่งคุยกับพี (และใครก็ตามที่รู้เรื่องระบบนี้แม้นิดเดียว)
เพื่อตอบคำถามพื้นฐานก่อน:

- ระบบนี้ทำอะไรกันแน่? user คือใคร? ใช้บ่อยแค่ไหน? ช่วงเวลาไหน peak?
- "เว็บช้า" หมายความว่ายังไง — ช้าทุกหน้า หรือช้าแค่บางฟังก์ชัน?
- ไฟล์แปลกใน server — อยู่ใน folder ไหน นานแค่ไหนแล้ว?
- feature ใหม่ที่ลูกค้าอยากได้ — urgent แค่ไหน? มี deadline ไหม?

**Plan B — ถ้าพีไม่รู้เรื่องระบบเลยจริงๆ:** ผมจะไม่หยุดรอ แต่เริ่มจาก server log
ย้อนหลัง + grep หา entry point หลักของ app เอง แล้วถามพีเฉพาะเรื่องที่ต้องการ
permission หรือ context ทางธุรกิจเท่านั้น

จากนั้น RDP เข้าไปดู server ทั้ง 3 เครื่อง **แบบดูอย่างเดียวก่อน** ไม่รัน command ใดๆ
เพื่อทำแผนที่ในหัว: เครื่องไหนคือ web server, เครื่องไหนคือ DB, deploy flow เป็นยังไง

**ข้อตกลงกับตัวเองระหว่าง discovery:** ผมจะไม่ทำอะไรกับ production ในช่วง peak hour
และจะแจ้งพีก่อนทุก action ที่มีความเสี่ยง แม้เล็กน้อย — เพราะ user ยังใช้ระบบนี้อยู่จริง
การพลาดแรงๆ ไม่ใช่แค่ปัญหา technical แต่กระทบคนจริง

**prompt ที่จะใช้กับ Claude วันแรก:**
> "ผมได้รับมอบหมายให้ดู legacy PHP web app บน Windows Server ที่ไม่มีเอกสาร
> ไม่มี test ทีมเดิมลาออกหมดแล้ว มีปัญหา 3 อย่าง: เว็บช้า, เจอไฟล์แปลกใน server,
> อยากเพิ่ม feature ใหม่ แต่ไม่มีใครกล้า ผมมี 2 สัปดาห์สำหรับ discovery
> ช่วยแนะนำว่าควรเริ่ม investigate อะไรก่อน และ checklist อะไรที่ควรทำ
> โดยไม่กระทบ production"

ผมจะใช้คำตอบจาก Claude เป็น starting point แล้วปรับตามสิ่งที่เห็นจริงในเครื่อง

---

### วันที่ 2 — ลำดับความสำคัญและเริ่ม investigate

ผมจะ prioritize 3 เรื่องแบบนี้:

**อันดับ 1: ไฟล์แปลก** → ก่อน เพราะถ้าเป็น malware หรือ backdoor
ทุกอย่างที่ทำหลังจากนี้อาจไม่มีความหมาย และอาจเพิ่มความเสี่ยงมากขึ้น

**อันดับ 2: เว็บช้า** → หลัง เพราะมีผลต่อ user ทุกวัน แต่ไม่ critical เท่าความปลอดภัย

**อันดับ 3: feature ใหม่** → สุดท้าย เพราะถ้าระบบยังไม่ stable การเพิ่ม feature
ใหม่คือการเพิ่มความเสี่ยงโดยไม่จำเป็น

วันนี้จะ focus ที่ไฟล์แปลก (ดูรายละเอียดในคำถามที่ 2)
และเริ่มดู server log คร่าวๆ เพื่อหาเบาะแสเรื่องความช้า

**prompt ที่จะใช้กับ Claude วันที่ 2:**
> "PHP web app บน Windows Server มีไฟล์ .php อยู่ใน folder images/
> ที่ code ถูก obfuscate ควรตรวจสอบอะไรก่อนที่จะตัดสินใจว่าคืออะไร
> และมีวิธีไหนที่ปลอดภัยในการวิเคราะห์ไฟล์นี้โดยไม่ run มัน?"

---

### วันที่ 3 — สรุปและ present แผนให้พีดู

จบวันที่ 3 ผมจะส่งมอบ:

1. **แผนที่ระบบ** (วาดมือก็ได้): 3 server ทำอะไร เชื่อมกันยังไง deploy flow เป็นยังไง
2. **Risk assessment เบื้องต้น**: ไฟล์แปลกน่าสงสัยแค่ไหน, ความช้าน่าจะมาจากไหน
3. **คำถามที่ยังไม่รู้**: ผมจะบอกตรงๆ ว่ายังไม่รู้อะไรบ้าง และจะหาคำตอบยังไง
4. **แผน 2 สัปดาห์**: สัปดาห์แรก discovery เพิ่มเติม สัปดาห์สองเขียน proposal

**สิ่งที่กลัวมากที่สุด:** รัน command ที่ไม่เข้าใจแล้วทำ DB เสีย
วิธีแก้: ทุก command ที่จะรัน ผมจะถามพีหรือ Claude ก่อนว่ามันทำอะไรกันแน่
และถ้าไม่แน่ใจ จะไม่รัน — ง่ายกว่าการแก้ปัญหาที่ตัวเองสร้างขึ้น

---

## คำถามที่ 2: "เจอไฟล์แปลก จะทำยังไง"

### ไฟล์ .php ใน folder images/ ที่ obfuscated — ผมคิดว่าคืออะไร

ตอบตรงๆ: ผมคิดว่าน่าจะเป็น **web shell** หรือ malicious backdoor มากกว่าจะเป็น
ไฟล์ที่ developer เดิมวางไว้โดยตั้งใจ เพราะ:

- ไฟล์ .php ไม่ควรอยู่ใน folder images/ ตามปกติ
- การ obfuscate code มักทำเพื่อซ่อนเจตนา ไม่ใช่เพราะ best practice
- ระบบอายุ 10 ปีที่ไม่มีใครดูแล คือเป้าหมายที่น่าโจมตี

**แต่ผมยังไม่ฟันธงว่าใช่แน่ๆ** เพราะยังไม่มีข้อมูลพอ

---

### คำถามที่ต้องรู้ก่อนตัดสินใจ

ก่อนทำอะไรเลย ผมจะถามพีว่า:

1. ไฟล์นี้อยู่มานานแค่ไหน? (ดูจาก file timestamp)
2. มี log ว่ามีการ access ไฟล์นี้จาก IP ภายนอกไหม?
3. มีไฟล์แปลกๆ อื่นอีกไหม หรือนี่เป็นอันเดียว?
4. ช่วงที่ไฟล์นี้ถูกสร้างขึ้น มีอะไรผิดปกติใน system log ไหม?
5. มี backup ของ server ก่อนหน้านี้ไหม?

---

### จะลบเลยไหม?

**ไม่ลบทันที** — เพราะ:

1. ถ้าลบแล้วไม่มี backup นั่นแปลว่าหลักฐานหายไปด้วย
   ถ้าภายหลังพบว่ามีการ breach จริง เราจะไม่มีอะไรให้วิเคราะห์
2. อาจมีไฟล์อื่นที่เกี่ยวข้องที่ยังไม่เจอ การลบแค่ไฟล์นี้อาจไม่ได้แก้ปัญหาจริง

**สิ่งที่จะทำแทน:**
1. Copy ไฟล์ไปเก็บใน folder ที่ web server เข้าไม่ได้ก่อน (เก็บไว้วิเคราะห์)
2. Rename ไฟล์ต้นฉบับให้ไม่สามารถรันได้ (เช่น .php → .php.bak)
3. ไม่ execute ไฟล์นี้เพื่อดูว่าทำอะไร — เสี่ยงเกินไป
4. ใช้ Claude วิเคราะห์ obfuscated code แบบ static analysis (paste โค้ดให้ดู โดยไม่รัน)

**prompt ที่จะใช้:**
> "นี่คือ PHP code ที่ถูก obfuscate พบใน folder images/ ของ web server
> ช่วยวิเคราะห์แบบ static ว่าโค้ดนี้น่าจะทำอะไร มีสัญญาณอะไรที่บ่งบอกว่า
> เป็น malicious code ไหม และถ้าใช่ควรทำขั้นตอนอะไรต่อ"

---

### ใครควรรู้เรื่องนี้

ผมจะแจ้งพีทันที และให้พีตัดสินใจว่าจะแจ้งลูกค้าหรือยัง
ไม่ใช่หน้าที่ผมในฐานะคนที่เพิ่งเข้ามา 3 เดือนที่จะตัดสินใจ escalation เอง

ถ้าพียืนยันว่าน่ากังวลจริง สิ่งต่อไปที่ต้องทำคือตรวจสอบว่า
ระบบถูก compromise ไปแล้วหรือยัง ไม่ใช่แค่ลบไฟล์แล้วจบ

---

## AI Journal (Part 4)

> *ดึงมาจาก AI_JOURNAL.md จริง — session ที่เกี่ยวข้องกับวิธีคิดแบบเดียวกับ legacy system*

---

### Session ที่เกี่ยวข้อง 1: Session 13 — เพิ่ม PostgreSQL ใน docker-compose

**ทำไมถึงเชื่อม:** สถานการณ์นี้คล้ายกับ legacy system มาก —
มี environment ที่ซับซ้อน, credentials ที่ต้องจัดการระวัง, และ dependency chain ที่พังแล้วหาสาเหตุยาก

**สิ่งที่เกิดขึ้น:** Claude Code เพิ่ม `DATABASE_URL: "postgresql://${POSTGRES_USER}:..."` ใน
`environment:` block ของ docker-compose ซึ่งทำให้ variable expansion เกิดจาก host shell
ไม่ใช่จาก `env_file` — ผลคือ password ว่างและ postgres ปฏิเสธ connection

Claude Code แก้โดย hardcode credentials ก่อน

**My Adjustment:** ผมสังเกตว่า hardcode credentials จะทำให้หลุดเข้า git
จึงสั่งให้แก้กลับเป็น `${VAR}` โดยให้ root `.env` อยู่ข้างๆ `docker-compose.yml`
ซึ่ง Compose อ่านอัตโนมัติ — แก้ปัญหาได้โดยไม่ต้อง hardcode

**เชื่อมกับ legacy thinking:** นี่คือตัวอย่างของการที่ AI แก้ปัญหาได้ แต่แก้แบบที่สร้างปัญหาใหม่
(credentials หลุด) — ถ้าเจอ legacy system จริง ก็ต้องคิดแบบนี้เสมอ: "แก้ได้ แต่แก้แบบนี้มีผลข้างเคียงอะไร?"

---

### Session ที่เกี่ยวข้อง 2: Session 14 — Git history ที่ไม่สมบูรณ์

**ทำไมถึงเชื่อม:** เจอ `.git` directory ที่มีอยู่เดิมแต่ไม่สมบูรณ์ (ไม่มี `objects/` และ `refs/`)
เพราะ GitKraken เขียนทับบางส่วน — นี่คือ "legacy state" ที่ต้องวิเคราะห์ก่อนแตะ

**สิ่งที่เกิดขึ้น:** Claude Code แนะนำให้ลบ `.git` แล้ว `git init` ใหม่ทันที

**My Adjustment:** ผมตรวจสอบก่อนด้วย `ls -la .git` และ `git cat-file` เพื่อยืนยันว่า
สูญหายจริงก่อนตัดสินใจลบ — ไม่ได้ลบทันทีตามที่ AI แนะนำแรก

**เชื่อมกับ legacy thinking:** ก่อนลบอะไรก็ตามใน legacy system ต้อง verify ก่อนเสมอ
เพราะสิ่งที่ดูเหมือน "ไม่มีประโยชน์" อาจมีข้อมูลที่ต้องการซ่อนอยู่ —
เหมือนกับไฟล์แปลกที่เจอใน images/ ผมจะไม่ลบโดยไม่ดูก่อน

---

### Session ที่เกี่ยวข้อง 3: Session 10/12 — Docker Compose ที่ผิด

**ทำไมถึงเชื่อม:** การที่ AI สร้าง config ผิดแล้วระบบดูเหมือนรันได้แต่จริงๆ มีปัญหาซ่อนอยู่
คือสถานการณ์ที่เจอบ่อยใน legacy system

**สิ่งที่เกิดขึ้น:** Claude Code สร้าง `docker-compose.yml` ที่มีข้อผิดพลาด 5 จุด —
`depends_on` ผิดทิศ, `NEXT_PUBLIC_API_URL` อยู่ผิด service, ไม่มี healthcheck,
และ YAML syntax ที่ผสม list กับ dict ทำให้ invalid

**My Adjustment:** ผม review ทีละจุด ระบุ error ทั้ง 5 และให้ corrected prompt ที่ชัดเจน
แทนที่จะ accept output แล้วค่อยมาแก้ทีหลัง และต้องการ raw output (`docker compose ps`,
`docker compose logs`, `curl`) ไม่ใช่ prose summary เพื่อยืนยันว่ารันได้จริง

**เชื่อมกับ legacy thinking:** ใน legacy system จะเจอ config ที่ "ดูถูก" แต่จริงๆ ผิด
บ่อยมาก — ต้องอ่านเอง verify เอง ไม่ trust AI output หรือ output ของ developer เดิม
โดยไม่ตรวจ

---

*เขียนโดย กัณฑศิษฎ์ ทิพย์สภาพกุล*
